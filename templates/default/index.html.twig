{% extends 'base.html.twig' %}

{% block body %}
   <section id="accueil">
      <div class="banner">
         <div class="typed-block">
            <p>Je suis <span class="typed"></span></p>
         </div>
      </div>
   </section>
   <section id="presentation">
      <div class="container">
         <div class="row">
            <div class="col-lg-3 col-md-4 col-12">
               <img class="rounded-circle" src="{{ vich_uploader_asset(user, 'pictureFile')|imagine_filter('index') }}" alt="">
            </div>
            <div class="col-lg-9 col-md-8 col-12 presentation-body">
               <h2>Je m'appelle Nom Prénom, je suis Développeur Backend sous le framework Php Symfony</h2>
               <p>
                  J'ai suivi une formation
                  <a href="https://openclassrooms.com/fr/paths/500-developpeur-dapplication-php-symfony" target="_blank">
                     "Développeur d'application Php/Symfony"
                  </a>
                  sur la plate-forme <b><a href="https://openclassrooms.com/fr/" target="_blank">Openclassroom</a></b>.
                  Lors de cette formation, j'ai appris à développer un site de A à Z, en passant par l'étude du projet,
                  les diagrammes UML, la conception et le déploiement. J'ai pu apprendre à développer sous le framework
                  Symfony 3 et 4.
               </p>
            </div>
         </div>
      </div>
   </section>
   <section id="blog">
      <div class="container">
         <h2>Derniers articles postés</h2>
         <div class="row row-cols-1 row-cols-xl-3 g-4">
            {% for article in articles %}
               <div class="col">
                  <div class="card">
                     <div class="card-header">
                        <h3 class="card-title">
                           <a href="{{ path('article_show', {'slug' : article.slug}) }}">{{ article.title }}</a>
                        </h3>
                     </div>
                     <div class="card-body">
                        {{ article.description|raw }}
                     </div>
                     <div class="card-read-more">
                        <a class="btn btn-info index-article-button" href="{{ path('article_show', {'slug' : article.slug}) }}">
                           Lire la suite
                        </a>
                     </div>
                     <div class="card-footer text-muted">
                        Publié le {{ article.publishedAt|localizeddate('none', 'none', null, null, 'dd MMMM Y à HH:mm') }}
                     </div>
                  </div>
               </div>
            {% endfor %}
         </div>
         <div class="index-blog-more">
            <a class="btn btn-info" href="{{ path('article_index') }}">Voir tous les articles</a>
         </div>
      </div>
   </section>
{% endblock %}

{% block javascripts %}
   {{ encore_entry_script_tags('typed') }}
{% endblock %}
